server:
  port: 9001

spring:
  application:
    name: cloud-nacos-server
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

#b暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: '*'



